__pycache__/
*.pyc
.venv/
data/staging/
data/canonical/
data/*.kuzu
data/*.kuzu.staging/
*.egg-info/

# Build artifacts / fetched data — regenerated by `mg fetch` / `mg build`, never committed.
snapshots/

# `mg bench` output (items/manifest) — regenerated by `mg bench`; committed separately once
# frozen, not by this build step. Anchored to repo root so it doesn't match the
# src/methods_graph/bench/ package.
/bench/

# macOS Finder metadata
.DS_Store
**/.DS_Store

# Third-party source bundles should be cited or fetched, not redistributed.
docs/arXiv-*.tar.gz

# Local agent/tooling scratch — never part of the public repo.
.superpowers/
.claude/
